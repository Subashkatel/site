import { NameTransition } from '../name';
import Link from 'next/link';

export const metadata = {
  title: 'Work',
  alternates: {
    canonical: '/work',
  },
};

export function Small({ children }) {
  return (
    <small className="flex text-sm text-gray-500 -mt-3 mb-3">{children}</small>
  );
}

<div className="max-w-2xl mx-auto px-6 py-12">
  # Work

  Here you can add your work experience, projects, or any other professional content you'd like to showcase.

  <Link href="/" className="text-blue-500 hover:text-blue-600">← Back to home</Link>
</div>
